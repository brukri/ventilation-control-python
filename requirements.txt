# Web framework and server
fastapi~=0.109.0  # Allows patches and minor updates (0.109.x) but not breaking changes
uvicorn~=0.27.0   # Allows patches and minor updates (0.27.x) but not breaking changes

# GPIO control (gpiozero automatically manages RPi.GPIO or pigpio as backend)
gpiozero~=2.0.1   # Modern GPIO interface with better abstractions